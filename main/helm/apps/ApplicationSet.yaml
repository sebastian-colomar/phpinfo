apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: phpinfo-helm
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - cluster: '1'
      - cluster: '2'
  syncPolicy:
    preserveResourcesOnDeletion: true
  template:
    metadata:
      labels:
        applicationset: phpinfo-provider-aws-helm
      name: phpinfo-provider-aws-helm-{{ cluster }}
    spec:
      destination:
        namespace: provider-aws-helm-{{ cluster }}
        server: 'https://kubernetes.default.svc'
      project: default
      source:
        helm:
          valueFiles:
          - provider-aws-helm-{{ cluster }}/values.yaml
        path: main/helm/
        repoURL: https://github.com/sebastian-colomar/phpinfo.git
        targetRevision: HEAD
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - ApplyOutOfSyncOnly=true
        - CreateNamespace=true
